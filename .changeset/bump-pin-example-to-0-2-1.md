---
'@getflute/sdk': patch
---

Bump the `--save-exact` pin example in `AGENTS.md` from `0.2.0` to `0.2.1` so the snippet pins to the version published with the file actually included in the tarball. No runtime changes.
